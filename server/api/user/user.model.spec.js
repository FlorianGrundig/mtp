'use strict';

var should = require('should');
var app = require('../../app');


describe('User Model', function() {

  it('should be true', function(done) {
      should(true).ok;
      done();
  });

});